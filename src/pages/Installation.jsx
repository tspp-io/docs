
import React from 'react';

export default function Installation() {
  return (
    <div className="container">
      <h1>Installation</h1>
      <ol>
        <li>Clone the tspp-compiler repository:
          <pre><code>git clone https://github.com/tspp-io/tspp-compiler.git</code></pre>
        </li>
        <li>Follow the README instructions in the repository to build and use the compiler.</li>
      </ol>
    </div>
  );
}
